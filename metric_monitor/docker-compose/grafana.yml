version: '3.8'
services:
  grafana:
    image: grafana/grafana-oss
    container_name: grafana
    user: root
    deploy:
      resources:
        limits:
          memory: 1g
    ports:
      - "3000:3000"
    restart: unless-stopped
